import Typography from '@mui/material/Typography';

export const HeaderFormPage = () => {
  return (
    <Typography
      variant={'h1'}
      align={'center'}
      sx={{
        fontSize: '30px',
        fontWeight: '700',
        marginTop: '10px',
        marginBottom: '40px',
      }}>
      Aplikuj na ofertę pracy
    </Typography>
  );
};